# QR处理器项目 - EXE打包指南

本指南将帮助您将QR处理器项目打包成独立的Windows可执行文件(.exe)。

## 📋 系统要求

- Windows 10/11 (64位)
- Python 3.8 或更高版本
- 至少 2GB 可用磁盘空间
- 管理员权限（用于安装依赖）

## 🚀 一键打包（推荐）

### 方法1: 使用批处理文件
1. 双击运行 `打包成EXE.bat`
2. 等待自动安装依赖和打包完成
3. 在 `dist` 文件夹中找到 `QR-Processor.exe`

### 方法2: 使用Python脚本
```bash
python build_exe.py
```

## 🔧 手动打包步骤

### 1. 安装打包依赖
```bash
pip install -r requirements-build.txt
```

### 2. 运行PyInstaller
```bash
pyinstaller --clean qr_processor.spec
```

### 3. 查找生成的文件
打包完成后，在 `dist` 文件夹中找到 `QR-Processor.exe`

## 📁 打包后的文件结构

```
dist/
└── QR-Processor.exe    # 主执行文件（约50-100MB）
```

## 🎯 使用打包后的EXE

1. **启动应用**：双击 `QR-Processor.exe`
2. **自动打开浏览器**：应用会自动在默认浏览器中打开 `http://127.0.0.1:5000`
3. **上传图片**：在网页界面中上传包含QR码的图片
4. **处理结果**：查看和下载处理后的QR码
5. **停止服务**：在控制台窗口按 `Ctrl+C` 或直接关闭窗口

## ⚙️ 打包配置说明

### PyInstaller配置 (qr_processor.spec)
- **入口文件**: `api/index.py`
- **包含资源**: `public` 文件夹（前端界面）
- **隐藏导入**: 所有必需的Python库
- **输出模式**: 单文件模式
- **控制台**: 显示（用于调试和状态信息）

### 包含的依赖库
- **Flask**: Web框架
- **OpenCV**: 图像处理
- **pyzbar**: QR码解码
- **qrcode**: QR码生成
- **Pillow**: 图像处理
- **numpy**: 数值计算

## 🐛 常见问题解决

### 问题1: 缺少依赖
**症状**: 运行时提示缺少某个模块
**解决**: 
```bash
pip install -r requirements-build.txt
pyinstaller --clean qr_processor.spec
```

### 问题2: 文件过大
**症状**: 生成的exe文件超过200MB
**解决**: 这是正常的，因为包含了完整的Python运行时和所有依赖

### 问题3: 启动缓慢
**症状**: 双击exe后需要等待较长时间
**解决**: 这是正常的，首次启动需要解压和初始化

### 问题4: 防病毒软件误报
**症状**: 防病毒软件阻止exe运行
**解决**: 将exe文件添加到防病毒软件的白名单

### 问题5: 端口被占用
**症状**: 提示端口5000被占用
**解决**: 
1. 关闭其他使用5000端口的程序
2. 或修改 `launcher.py` 中的端口号

## 📦 分发说明

### 单文件分发
生成的 `QR-Processor.exe` 是一个独立的可执行文件，可以直接分发给其他用户，无需安装Python或其他依赖。

### 系统要求（最终用户）
- Windows 10/11 (64位)
- 无需安装Python
- 无需安装其他依赖

### 文件大小优化
如果需要减小文件大小，可以考虑：
1. 使用 `--exclude-module` 排除不必要的模块
2. 使用目录模式而非单文件模式
3. 使用UPX压缩（已在spec中启用）

## 🔄 更新和维护

### 更新应用
1. 修改源代码
2. 重新运行打包脚本
3. 替换旧的exe文件

### 版本管理
建议在文件名中包含版本号：
```
QR-Processor-v1.0.0.exe
QR-Processor-v1.1.0.exe
```

## 📞 技术支持

如果在打包过程中遇到问题，请检查：
1. Python版本是否兼容
2. 所有依赖是否正确安装
3. 磁盘空间是否充足
4. 是否有足够的系统权限

---

**注意**: 打包后的exe文件较大（50-100MB）是正常现象，因为它包含了完整的Python运行时环境和所有必需的库文件。